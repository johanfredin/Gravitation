Gravitation
===========

A LibGDX game built with Gradle.

## Requirements

- JDK 11+

## Build

```
./gradlew build
```

(On Windows, use `gradlew.bat` instead of `./gradlew`.)

## Run

```
./gradlew run
```

This launches the desktop version (`Gravitaion-desktop`).
